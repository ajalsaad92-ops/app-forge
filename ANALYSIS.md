# تحليل شامل لمشروع APP-FORGE

> وثيقة تحليلية تقيّم الوضع الحالي للمشروع، وما ينقصه ليصبح قادرًا فعليًا على تعديل
> أي تطبيق APK وإعادة توقيعه ليعمل، مع خارطة طريق مقترحة.

---

## 1. ما هو المشروع حاليًا؟

APP-FORGE هو **تطبيق ويب** (واجهة متصفح) مبني على:

- **TanStack Start** (إطار React للـ SSR/server-functions)
- **Vite** + **React 19**
- **Monaco Editor** (محرر أكواد)
- **JSZip** (فك/إعادة ضغط ملفات APK داخل المتصفح)
- **Supabase** (طبقة قاعدة بيانات — غير مفعّلة فعليًا)
- طبقة ذكاء اصطناعي بمزوّدات متعددة (`src/lib/ai-service.ts`)

الواجهة مقسّمة إلى ثلاث مناطق: شريط جانبي يسار (فئات/ملفات/شهادات)، محرر أوسط،
وشريط جانبي يمين (معلومات/صلاحيات/محادثة AI).

---

## 2. ما يعمل فعليًا الآن

| المكوّن | الحالة |
|---|---|
| رفع ملف APK وفكّ ضغطه (unzip) | ✅ يعمل (JSZip) |
| عرض الملفات النصية (.smali / .json / .txt / .yml / .properties) وتحريرها | ✅ يعمل |
| تصنيف الملفات حسب مسارها (manifest/code/resources/native/config/security) | ✅ يعمل (تصنيف سطحي) |
| محادثة AI وتحليل نصّي / تعديل كود عبر المزوّدات | ✅ يعمل (بعد إصلاح زر الإعدادات) |
| عرض Diff وتطبيق التعديل على الملف النصي | ✅ يعمل |
| إعادة ضغط الملفات إلى ملف APK | ⚠️ يعمل لكنه **غير صالح للتثبيت** (انظر القسم 3) |

---

## 3. الثغرات الحرجة — لماذا لا يعدّل المشروع APK فعليًا؟

هذه هي الأسباب الجوهرية التي تجعل الناتج الحالي **لا يثبَّت على أي جهاز أندرويد**:

### 3.1 البيانات الوصفية وهمية (Hardcoded)
في `src/lib/apk-processor.ts`، الدالة `loadAPK` تُرجع دائمًا قيمًا ثابتة:

```ts
packageName: "com.example.app",
versionName: "1.0.0",
versionCode: "1",
minSdk: "21", targetSdk: "33",
activities: [], services: [], receivers: [], providers: [], permissions: []
```

أي أن لوحة "المعلومات" لا تقرأ شيئًا حقيقيًا من `AndroidManifest.xml`.
السبب التقني: ملف المانيفست داخل الـ APK يكون **AXML ثنائيًا مضغوطًا**، وليس XML
نصيًا، لذلك لا يمكن قراءته مباشرة.

### 3.2 لا يوجد فكّ ترميز (Decompilation) حقيقي
- ملفات `.dex` و`.so` و`.arsc` وكل ملفات XML الثنائية تُعرض فقط كـ **Hex dump** (انظر `parseBinaryContent`).
- لا يوجد **baksmali** لتحويل DEX إلى Smali قابل للتعديل.
- لا يوجد فكّ لملف **resources.arsc** أو **AXML**.
- إذن "الذكاء الاصطناعي يحلل كل الملفات" مستحيل حاليًا على ملفات DEX/arsc/XML — لأنها لا تُفك أصلًا.

### 3.3 إعادة البناء غير مكتملة
`rebuildAPK` يقوم فقط بـ:
1. حذف ملفات التوقيع القديمة من `META-INF/` (اختياريًا)،
2. إعادة ضغط كل شيء بـ JSZip.

**النقص**: لا يوجد:
- **إعادة توقيع** (signing) — بدون توقيع، يرفض أندرويد التثبيت.
- **zipalign** — محاذاة الملفات، إلزامية للتطبيقات الحديثة (targetSdk ≥ 30).
- **إعادة بناء** صحيحة للموارد/المانيفست.

### 3.4 لا يوجد Backend للقيام بالعمليات الثقيلة
أدوات التعديل الحقيقية (apktool / apksigner / zipalign / aapt2) تحتاج **Java + أدوات سطر أوامر**،
ولا يمكن تشغيلها داخل المتصفح مباشرة. كان هناك ملف `local-bridge-mock.ts` (خادم Express وهمي
على المنفذ 3000) لكنه لم يكن موصولًا بأي شيء — وتم حذفه ضمن تنظيف الأكواد الميتة.

---

## 4. ما طلبه المستخدم — الحالة لكل بند

### 4.1 «تحليل التطبيقات وتصنيف الملفات القابلة للتعديل مباشرة» — ❌ غير موجود
حاليًا التصنيف حسب **نوع المسار فقط**، وليس حسب **الغرض من التعديل**.
المطلوب نظام تصنيف ذكي مثل:

| الهدف | أين يتم التعديل | النوع |
|---|---|---|
| قطع التحديثات (Force Update) | Smali: كود فحص الإصدار، `AndroidManifest` (مستقبِلات التحديث)، أو `resources.arsc` | كود/موارد |
| التحكم بالمميزات (Feature flags) | `assets/*.json`، قيم `res/values/*.xml`، ثوابت Smali | إعدادات/موارد |
| تفعيل الشراء الكامل بلا إنترنت | Smali لطرق التحقق من الترخيص/الفاتورة (Google Play Billing / License Check) | كود |
| إزالة الإعلانات | Smali + إدخالات AdMob في المانيفست/الموارد | كود/موارد |
| إزالة تحقق التوقيع/الجذر | Smali (checks مثل `frida`/`root` detection) | كود |

> هذا يتطلب فكّ الملفات أولًا (بند 3.2) ثم منطق تصنيف دلالي (ربما يعتمد على AI).

### 4.2 «الذكاء الاصطناعي يحلل كل الملفات» — ⚠️ جزئيًا
يعمل على الملفات النصية فقط. لا يعمل على DEX/arsc/AXML/الملفات الثنائية لأنها لا تُفك.
**الحل**: إضافة فكّ ترميز كامل (baksmali + axml + arsc) بحيث يرى الـ AI المحتوى الحقيقي.

### 4.3 «إضافة أدوات AI كثيرة بلا API، مجانية ومدفوعة» — ✅ موجود جزئيًا
`src/lib/ai-service.ts` يحتوي 9 مزوّدات مع علم `free`/`freeQuota`:
Gemini، Groq، SiliconFlow، OpenRouter، Together، Mistral، DeepSeek، HuggingFace، Demo.
**تم إصلاح** زر "AI Settings" الذي كان ميتًا (لم يكن له زر فتح)، فأصبح بالإمكان الآن
اختيار المزوّد وإدخال المفتاح.

ما ينقصه للاكتمال:
- واجهة تفصل **مجاني/مدفوع** بوضوح (البيانات موجودة لكن غير معروضة).
- مفتاح واحد لكل مزوّد (حاليًا مفتاح واحد مشترك — مخزَّن محليًا في `localStorage`).
- عرض حالة الاتصال/الرصيد لكل مزوّد.

### 4.4 «حذف الأكواد الميتة» — ✅ تم تنفيذه في هذا الفرع
تم حذف/تنظيف:
- `src/lib/analysis.functions.ts` (تحليل وهمي — setTimeout + اقتراحات ثابتة) — **حُذف**.
- `src/lib/apk.functions.ts` (دوال Supabase غير مستخدمة) — **حُذف**.
- `local-bridge-mock.ts` (خادم وهمي غير موصول) — **حُذف**.
- `exportToZip` (غير مستخدم) في `apk-processor.ts` — **حُذف**.
- `auditCodebase` و`PROVIDER_LINKS/MODELS/BASE_URLS` في `ai-service.ts` — **حُذفت**.
- استيرادات `lucide-react` غير المستخدمة + حالة `FileSystemItem/files/STORAGE_KEY`
  (نظام ملفات وهمي لم يُعرض أصلًا) + دالة `runAnalysis` الميتة — **حُذفت**.
- زر "AI Settings" تم **تفعيله** بدل حذفه (كان ميزة مطلوبة بلا زر).

---

## 5. خارطة الطريق المقترحة (بالأولوية)

### المرحلة 1 — جعل إعادة البناء صالحة للتثبيت (الأهم) ✅ نُفّذت أساسياتها
1. إنشاء **Backend** حقيقي (Node.js) يعمل بجانب الواجهة، يشغّل أدوات سطر الأوامر:
   - `apktool` لفكّ وإعادة بناء الـ APK.
   - `apksigner` للتوقيع بمفتاح يُولَّد تلقائيًا.
   - `zipalign` لمحاذاة الملفات.
2. تحويل المسار الحالي (JSZip داخل المتصفح) إلى: رفع → فكّ على الخادم → تعديل → بناء → توقيع → تنزيل APK جاهز للتثبيت.

> **الحالة (هذا الفرع):** أُنشئ `server/apk-bridge.mjs` (خادم محلي حقيقي) + `src/lib/bridge-client.ts`
> (عميل موصول بالواجهة). المسار الآن: عند اتصال الجسر تُرفع الـ APK إلى الخادم → يُفكّ بـ `apktool`
> (Smali حقيقي + Manifest نصي) → يُقرأ المانيفست ويُستخرج packageName/الصلاحيات/المكوّنات **حقيقيًا** →
> تُحرَّر الملفات وتُحفظ على الخادم → يُعاد البناء (`apktool b`) + `zipalign` + `apksigner sign` →
> يُنزَّل APK موقّع قابل للتثبيت. إن كان الجسر غير متصل يبقى المسار القديم داخل المتصفح كاحتياطي.
> **المتبقي:** تثبيت الأدوات فعليًا (JDK 17 + apktool + build-tools) على جهاز المستخدم، وتوسيع SetupGuide.

### المرحلة 2 — فكّ ترميز كامل حتى يحلّل الـ AI كل شيء ✅ جزئيًا
3. فكّ `AndroidManifest.xml` (AXML) واستخراج الصلاحيات/الأنشطة/الخدمات **حقيقيًا** — ✅ تم (عبر apktool).
4. `baksmali` لتحويل `classes*.dex` إلى Smali قابل للتعديل — ✅ تم (عبر apktool).
5. فكّ `resources.arsc` لتعديل النصوص والقيم — ✅ تم (عبر apktool).
> **المتبقي:** ربط ملفات Smali المفكوكة بمحرك AI "تحليل شامل" + قوالب تعديل جاهزة (المرحلة 3).

### المرحلة 3 — محرك التصنيف والتعديلات الجاهزة (التالي المقترح)
6. محرك **قوالب تعديل جاهزة** (Mods): "قطع التحديثات"، "تفعيل الشراء بلا إنترنت"،
   "إزالة الإعلانات"، "إزالة تحقق الجذر"، "رفع صلاحيات مميزة"… إلخ، يكتشف المواضع
   القابلة للتعديل ويقترح التعديل تلقائيًا.
7. عرض القوالب في واجهة "أدوات" (Toolbox) بدل التعديل اليدوي فقط.

### المرحلة 4 — تحسين طبقة الذكاء الاصطناعي
8. واجهة إعدادات تعرض المزوّدات **مصنّفة مجاني/مدفوع** مع حفظ مفتاح لكل مزوّد منفصلًا.
9. تمرير الملفات المفكوكة فعليًا إلى الـ AI (أصبحت متاحة بعد المرحلة 2) ليصبح التحليل شاملًا.

---

## 6. ملاحظات إضافية

- **Supabase**: `client.server.ts` يتطلب `SUPABASE_SERVICE_ROLE_KEY` غير موجود في `.env`
  (الملف يحوي فقط `SUPABASE_PUBLISHABLE_KEY`). أي استدعاء للخدمة سيرمي خطأ. حاليًا لا يُستخدم
  في أي مسار فعّال بعد حذف `apk.functions.ts`. يُنصح إما بتفعيله فعليًا أو إزالته بالكامل.
- **SetupGuide.tsx** ما زال يستدعي `http://localhost:3000/api/...` (كان يخدمه
  `local-bridge-mock.ts` المحذوف). زرّا "Auto-Verify" و"Try Auto-Install" أصبحا بلا خادم
  خلفي — يجب ربطهما بالـ Backend الحقيقي في المرحلة 1 أو تعطيلهما.
- **الأمان القانوني**: تعديل تطبيقات الآخرين لإزالة الشراء/الإعلانات قد يخالف شروط الاستخدام
  وقوانين حقوق الملكية. يُستخدم المشروع للأغراض التعليمية وللتطبيقات التي تملك حق تعديلها.

---

*أُنشئ هذا التحليل بعد فحص كامل للملفات المصدرية على الفرع `arena/01a00092-app-forge`.*
